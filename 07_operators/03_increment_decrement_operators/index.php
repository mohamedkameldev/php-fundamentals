<?php


/*
 *
 * Increment/Decrement Operators: quick way to increase and decrease the value of a variable by 1.
 *
 * ++$var (Pre-increment).
 * $var++ (Post-increment).
 * --$var (Pre-decrement).
 * $var-- (Post-decrement).
 */
