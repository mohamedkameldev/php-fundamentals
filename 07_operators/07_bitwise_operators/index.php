<?php


/*
 * Bitwise Operators:
 *      used to perform bit-level operations.
 *      perform operations on the binary representation of the operands.
 *
 * & (And).
 * | (Or).
 * ^ (Xor).
 * ~ (Not).
 * << (Shift left).
 * >> (Shift right).
 */
