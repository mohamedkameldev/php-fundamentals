<?php

/**
 * arrow functions:
 *  are more concise syntax for the anonymous functions.
 *  provides a shorter syntax for writing a short anonymous function.
 *
 *  can have only one expression and return this expression,
 *  (there is no return keyword - it doesn't return anything).
 *
 *  can automatically access variables from their parent scopes.
 *
 * syntax: fn (arguments) => expression;
 */
